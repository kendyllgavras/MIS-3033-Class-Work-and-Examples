@model IEnumerable<MVC_EF.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


@foreach (var item in Model)
{
    <div class="card" style="width: 18rem;">
        <img src="..." class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title">   @Html.DisplayFor(modelItem => item.FirstName)    @Html.DisplayFor(modelItem => item.FirstName) </h5>
            <p class="card-text">    @Html.DisplayFor(modelItem => item.FavoriteColor)</p>
            <a href="#" class="btn btn-primary">Go somewhere</a>
        </div>
    </div>

}